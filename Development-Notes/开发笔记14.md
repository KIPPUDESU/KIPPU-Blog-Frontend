style(ui): refine article card styles and alignments

### 主要变更

本次提交主要对 UI 样式进行了一系列微调，旨在提升页面的视觉效果和一致性。

- **文章卡片 (`ArticleCard.vue`)**
    - **悬浮效果调整:** 将鼠标悬浮时的放大效果从 `1.06` 倍降低为 `1.04` 倍，使交互更显细腻。
    - **背景色优化:** 调整了卡片在不同状态下的背景色和透明度，优化了视觉过渡。
    - **布局微调:** 调整了卡片内文字区域的高度和对齐方式，使内容展示更规整。

- **首页 (`index.vue`)**
    - **样式修正:** 修正了分隔线上一个装饰性圆点的精确位置，实现了更好的视觉对齐。

- **内容 (`04.mantras.md`)**
    - **格式修正:** 为 `mantras` 分享页面的元数据描述添加了引号，确保格式正确。

---
**标题行中文翻译：**

样式(UI)：优化文章卡片样式与对齐
