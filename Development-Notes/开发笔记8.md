# 除法自动转为浮点  
10.0 / 3  # => 3.3333333333333335
- 雙精度浮點近似後，Python 顯示最短可回轉的 3.3333333333333335
- 大約能準確到 15–17 位十進位數
  
# 整数除法的结果都是向下取整  
-5.0 // 3.0  # => -2.0  
**以上示例包含了浮点、负数**  
  
# 模除  
-7 % 3 # => 2  
- 运算依据：`-7 / 3 ≈ -2.333… ⇒ floor(-2.333…) = -3`  
- 其次： i % j 结果的正负符号会和 j 相同，而不是和 i 相同
  
# 使用逻辑运算符对数字类型值进行运算，会强制转换为布尔值进行运算
**`and` 運算子的回傳規則**  
   * `A and B` 先評估 `A`： 
     * 如果 `A` 是假值（False），立刻回傳 **`A`**，不再看 `B`。  
     * 否則（`A` 為真值），回傳 **`B`**（最後一個運算值）。    
   ```python  
   0 and 2    # A = 0（假值），立刻回傳 0    
   5 and 2    # A = 5（真值），回傳 B = 2    
   ```  
- 示例：`2 < 3 and 3 < 2  # => False`
**`or` 運算子的回傳規則**    
   * `A or B` 先評估 `A`：  
     * 如果 `A` 是真值，立刻回傳 **`A`**，不再看 `B`。  
     * 否則（`A` 為假值），回傳 **`B`**。  
   ```python  
   -5 or 0    # A = -5（真值），立刻回傳 -5    
   0 or 4     # A = 0（假值），回傳 B = 4    
   ```  
  
# 有关字符串  
## 获取长度的 len
len("This is a string")  # => 16  
## 使用 f-strings 格式化字符串（python3.6+）
```
name = "Reiko"
f"She said her name is {name}." # => "She said her name is Reiko"
```  
大括号内可加入几乎任何 python 表达式，表达式的结果会以字符串的形式返回
```
f"{name} is {len(name)} characters long." # => "Reiko is 5 characters long."
```  
  
## .format 格式化字符串
```
"{} can be {}".format("strings", "interpolated")
# 可以重复参数以节省时间
"{0} be nimble, {0} be quick, {0} jump over the {1}".format("Jack", "candle stick")  
# 可以用关键字
"{name} wants to eat {food}".format(name="Bob", food="lasagna")
```  
> 占位符 `{}`
> 字串裡的兩個 `{}` 就是「佔位符」，表示「這裡要放東西」。
>
> 呼叫 `.format()`
>
> `"{} can be {}".format("strings", "interpolated")`
>
> `format()` 方法接收兩個參數：
>
> 1. `"strings"` 對應到第一個 `{}`
> 2. `"interpolated"` 對應到第二個 `{}`  
  
# 关于输出  
## 可以使用附加参数改变输出结尾
```
print("Hello, World", end="!")  # => Hello, World!
```
## 从终端拿数据：  
```
input_string_var = input("Enter some data: ") # 返回字符串数值
```  
  
# 变量
## python写变量不需要提前声明
## 习惯小写命名法并用下划线分割

# 关于列表（list）
## append追加元素
```
li.append(1)    # li现在是[1]
li.append(2)    # li现在是[1, 2]
li.append(4)    # li现在是[1, 2, 4]
```

# 零碎的常识：
## 双乘就是次方的意义  
```  
2**4 # => 16   
```  
  
## python里可以用括号决定优先级  
```  
(1 + 3) * 2  # => 8  
```  
  
## 语义化的逻辑运算，python的特征之一，大多数语言使用符号而非语义
- 使用 not 作布尔值取非 （非）
- 逻辑运算符的 and 和 or （与或）
- (is 对比 ==) is 判断两个变量是否引用同一个对象
- 符號版（&&/||/!）在 C++ 社群更常見  

## 三元运算
A if 條件 else B  
```
x = 10
result = "大" if x > 5 else "小"
print(result)
```  
  
## 大小比较可以连起来  
```  
2 < 3 < 2  # => False  
```  
  
## None是一个对象  
- None 进行比较时不用 ==，用 is  
-除了与“空”相关，其他值都是 True  



